<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.choi.Planit.org.user.mapper.UserMapper">

    <insert id="join" parameterType="io.choi.Planit.org.user.domain.Users">
        <selectKey keyProperty="user_id" resultType="int" order="BEFORE">
            select seq_users.nextval from dual
        </selectKey>        
        insert into users (
            user_id, name, dept_id, dept_name, status, pos_id, rank_id, password, create_dt , manager_f
        ) values (
            #{userId}, #{name}, #{deptId}, #{deptName}, #{status}, #{posId}, #{rankId}, #{password}, #{createDt}, #{managerF}
        )
    </insert>

    <select id="findBySabun" resultType="io.choi.Planit.org.user.domain.Users">
        select * from users where sabun = #{sabun}
    </select>   

    <select id="findByUsers" resultType="io.choi.Planit.org.user.domain.Users">
        select * from users where name = #{name} and sabun = #{sabun}
    </select>

    <update id="passWordReset">
        update users 
            set password = #{password} , password_update_dt = sysdate
        where name = #{name} and sabun = #{sabun}
    </update>
</mapper>
